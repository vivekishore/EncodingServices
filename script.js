function loadServices() {
  var servicesContent = document.getElementById("servicesContent");
  // Toggle visibility of services content
  if (servicesContent.style.display === "none") {
    servicesContent.style.display = "block";
    // Add logic to load content dynamically here, e.g., AJAX request, etc.
  } else {
    servicesContent.style.display = "none";
  }
}

function loadAlarms() {
  var alarmsContent = document.getElementById("alarmsContent");
  // Toggle visibility of alarms content
  if (alarmsContent.style.display === "none") {
    alarmsContent.style.display = "block";
    // Add logic to load content dynamically here, e.g., AJAX request, etc.
  } else {
    alarmsContent.style.display = "none";
  }
}

function loadSupport() {
  var supportContent = document.getElementById("supportContent");
  // Toggle visibility of support content
  if (supportContent.style.display === "none") {
    supportContent.style.display = "block";
    // Add logic to load content dynamically here, e.g., AJAX request, etc.
  } else {
    supportContent.style.display = "none";
  }
}

function loadSystem() {
  var systemContent = document.getElementById("systemContent");
  // Toggle visibility of system content
  if (systemContent.style.display === "none") {
    systemContent.style.display = "block";
    // Add logic to load content dynamically here, e.g., AJAX request, etc.
  } else {
    systemContent.style.display = "none";
  }
}
